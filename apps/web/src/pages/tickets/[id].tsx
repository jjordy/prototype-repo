import Layout from "@/components/Layout";
import TicketById from "@/features/ticket/TicketById";

export default function TicketByIdPage() {
  return (
    <Layout>
      <TicketById />
    </Layout>
  );
}
